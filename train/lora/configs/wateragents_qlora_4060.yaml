### model
model_name_or_path: D:/hf_cache/models--Qwen--Qwen3-4B/snapshots/1cfa9a7208912126459214e8b04321603b3df60c
trust_remote_code: true
quantization_bit: 4
quantization_method: bnb
double_quantization: true
upcast_layernorm: true

### method
stage: sft
do_train: true
finetuning_type: lora
lora_rank: 8
lora_target: all

### dataset
dataset: wateragents_sft
eval_dataset: wateragents_sft_val
dataset_dir: D:/AgentProject/WaterAgents/train/lora/llamafactory_data
template: qwen3_nothink
cutoff_len: 1024
max_samples: 100000
gradient_checkpointing: true
preprocessing_num_workers: 1
dataloader_num_workers: 0

### output
output_dir: saves/wateragents/qlora/sft
logging_steps: 10
save_steps: 500
plot_loss: true
overwrite_output_dir: true
save_only_model: false
report_to: none

### train
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 1.0e-4
num_train_epochs: 3.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
ddp_timeout: 180000000
resume_from_checkpoint: null

### eval
per_device_eval_batch_size: 1
eval_strategy: "no"
